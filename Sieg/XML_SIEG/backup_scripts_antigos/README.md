# Backup dos Scripts Antigos

Esta pasta contém os scripts antigos que foram substituídos pela nova estrutura modular refatorada.

## Arquivos em Backup

1. **baixar_xmls_unificado.py** → Substituído por `scripts/baixar_xmls.py`
2. **enviar_xmls_sieg.py** → Substituído por `scripts/enviar_xmls.py`
3. **extrair_chaves_xml.py** → Substituído por `scripts/extrair_chaves.py`
4. **organizar_xmls.py** → Substituído por `scripts/organizar_xmls.py`
5. **requisicao_api.py** → Script de teste, não mais necessário

## Data do Backup

Backup criado após refatoração completa do projeto em estrutura modular.

## Nota

Estes arquivos foram movidos para backup por segurança. Se precisar restaurar algum deles, basta copiá-los de volta para a raiz do projeto.

Os novos scripts estão em `scripts/` e usam a estrutura modular em `src/sieg_xml/`.
