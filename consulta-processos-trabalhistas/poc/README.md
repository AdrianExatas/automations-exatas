# POC Datajud

Valida se a API pública localiza processo trabalhista por CNPJ.

```bash
python -m pip install -r ../requirements.txt
python inspect_schema.py
python search_cnpj.py
```

Amostra em `amostra.csv`. Relatório em `RESULTADO.md`.
