# JWT do Gestta (sem o prefixo "JWT "). Preferencialmente use o artefato do shared/onvio-auth.
JWT_GESTTA=

# Caminho opcional da matriz de parametrizacao.
# MATRIX_PATH=PLANILHA GERAL TAREFAS POR REGIME (4) (1).xlsx

# Caminho opcional do artefato gerado por shared/onvio-auth.
# ONVIO_AUTH_ARTIFACT_PATH=

# Timeouts/retries Gestta.
# GESTTA_HTTP_TIMEOUT_MS=60000
# GESTTA_READ_RETRIES=5
# GESTTA_READ_RETRY_DELAY_MS=2000
