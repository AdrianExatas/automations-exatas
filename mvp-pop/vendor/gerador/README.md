# Vendor gerador-pop-it

Cópia dos scripts, schema content-v2, padrão documental e templates usados pelo MVP.

Os binários (`vendor/ffmpeg`, `vendor/whisper`) **não** são versionados aqui. Crie a junction:

```powershell
bun run setup:vendor
```

Origem esperada: `../gerador-pop-it/skills/gerar-pop-it/vendor`.
